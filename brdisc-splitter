#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CLI="$SCRIPT_DIR/Sources/BRDiscSplitter/Resources/brdisc-splitter"

if [ ! -f "$CLI" ]; then
  printf 'Error: bundled CLI script not found: %s\n' "$CLI" >&2
  exit 127
fi

exec /bin/bash "$CLI" "$@"
